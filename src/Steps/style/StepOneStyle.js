import styled from "styled-components";

const StepOneDiv = styled.div`
  width: 100%;
  background: var(--color-white);

  .movieBox {
    margin-bottom: 1rem;

    p {
      margin: 0;
    }
  }
`;

export default StepOneDiv;
